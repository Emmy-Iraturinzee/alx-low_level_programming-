#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @n: number being tested
 * Return: return with conditions
 */

int _abs(int n)
{
if (n < 0)

return (n * -1);

								}
return (n);
}

